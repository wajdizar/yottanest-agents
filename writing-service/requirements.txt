# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP
httpx>=0.27.0
sse-starlette>=2.1.0

# Logging
structlog>=24.4.0
python-json-logger>=2.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-timeout>=2.3.0

# Note: LangChain dependencies are provided by DeerFlow harness
# The writing-service integrates with DeerFlow's model factory
# which is installed via: cd ../backend && uv pip install -e packages/harness
